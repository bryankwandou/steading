{
  "name": "steading-vercel",
  "version": "1.0.0",
  "private": true,
  "description": "Steading, hosted build. Fast. Seamless. 100% Local.",
  "type": "module",
  "dependencies": {},
  "scripts": {
    "dev": "node scripts/serve.js",
    "test": "node --test tests/*.test.js",
    "test:py": "python tests/test_api.py",
    "package": "node scripts/package.js && node scripts/make-verify.js && node scripts/make-sites.js && node scripts/stamp-sw.js",
    "deploy": "node scripts/stamp-sw.js && vercel deploy --prod --yes"
  }
}
